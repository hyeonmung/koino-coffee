# KOI COFFEE — 도메인 연결 전 런칭 체크리스트

실제 도메인에 연결해 공개하기 전, 운영자가 직접 확인해야 하는 항목입니다. 코드 작업이
필요한 항목과 운영자가 직접 입력만 하면 되는 항목을 구분했습니다.

## A. 운영자가 지금 바로 채워야 하는 실제 정보

이 항목들은 개발자가 대신 채울 수 없습니다 — 가짜 데이터를 넣지 않는다는 원칙(데이터
정직성) 때문에 지금은 모두 비어 있습니다. `/admin/settings`(사이트 설정)에서 입력하세요.

- [ ] **실제 주소** — 매장 또는 사업장 주소
- [ ] **실제 전화번호**
- [ ] **실제 운영시간** (예: "매일 10:00 - 20:00", "매주 월요일 휴무" 등)
- [ ] **사업자 정보** — 상호, 대표자명, 사업자등록번호 (통신판매업 신고 시 신고번호도 함께)
- [ ] **Instagram URL**
- [ ] **Naver(스마트플레이스 등) URL**
- [ ] **온라인 구매 링크** (자체 쇼핑몰, 스마트스토어 등 실제 결제가 이루어지는 곳)
- [ ] **납품·교육 페이지 URL** (기본값 `/business` 그대로 사용해도 무방)
- [ ] **Footer 저작권 문구** (기본값 있음 — 상호명만 실제와 일치하는지 확인)
- [ ] **실제 원두/로스팅/매장/사람 사진** — Hero, 원두별 대표 이미지, About 섹션, Story
      커버, Character 이미지. 없는 항목은 KOI 플레이스홀더로 자동 대체되므로 급하지
      않은 것부터 순서대로 채워도 됩니다.
- [ ] **관리자 비밀번호 변경** (기본값 `8001`) — 개발자에게 `src/constants/auth.ts`의
      `ADMIN_PASSWORD` 값을 바꿔달라고 요청

## B. 개발자/기술 담당이 도메인 연결 시 함께 처리해야 하는 항목

- [ ] **도메인 연결 (Domain)** — 정적 호스팅(Netlify/Vercel/Cloudflare Pages 등)에 `dist/`
      업로드 후 커스텀 도메인 연결. `HashRouter`를 쓰므로 별도 SPA rewrite 설정 불필요.
- [ ] **SSL** — 대부분의 정적 호스팅은 커스텀 도메인 연결 시 SSL을 자동 발급함. 발급 완료
      및 `https://` 강제 리다이렉트 확인.
- [ ] **sitemap.xml의 도메인 교체** — `public/sitemap.xml`의 `https://example.com`을 실제
      도메인으로 전부 교체.
- [ ] **robots.txt의 Sitemap 줄 교체** — `public/robots.txt`의 `Sitemap:` 줄도 동일하게 교체.
- [ ] **favicon 확인** — `public/favicon.svg`가 실제 브랜드 로고와 일치하는지 최종 확인.
- [ ] **OG 기본 이미지** — `/admin/settings`의 "OG 이미지 URL"에 카카오톡/SNS 공유 시 보일
      대표 이미지 등록(1200×630 권장).
- [ ] **Analytics 연결** — GA4/네이버 애널리틱스 등 원하는 도구의 스크립트를 `index.html`에
      삽입 (현재 삽입 지점 없음 — 개발자 작업 필요).
- [ ] **정기 백업 계획 수립** — 현재 데이터는 브라우저 LocalStorage에만 있습니다. 관리자
      컴퓨터의 브라우저 데이터가 삭제되면 모든 원두/스토리/설정이 사라집니다. 최소한
      주기적으로 개발자 도구 콘솔에서 `localStorage`를 export하거나, Supabase 등 실제
      백엔드로 전환하는 일정을 잡으세요 (README "데이터베이스로의 전환" 참고).
- [ ] **관리자 계정 분리 여부 결정** — 지금은 비밀번호 하나를 전 직원이 공유합니다. 직원별
      권한 분리가 필요하면 Supabase Auth 연결이 선행되어야 합니다.

## C. 배포 직전 마지막 확인 (운영자가 직접 클릭해서 확인)

- [ ] `npm run build`가 에러 없이 끝나는지 최종 확인 (개발자 담당)
- [ ] 관리자로 로그인해 원두 1개를 새로 등록 → 공개 → 실제 화면 반영 확인
- [ ] 헤더 검색창에 아무 원두 이름이나 입력해 검색이 되는지 확인
- [ ] 납품·교육 문의 폼을 실제로 한 번 제출해보고 관리자 "문의 관리"에 뜨는지 확인
- [ ] Footer의 관리자 링크가 공개 메뉴에는 없고 Footer에만 있는지 확인
- [ ] 실제 휴대폰으로 사이트를 열어 최소 홈/원두/원두 차트 화면이 깨지지 않는지 확인
      (이번 세션은 브라우저 자동화 도구의 한계로 모바일 스크린샷 검증을 못 했으므로,
      배포 전 실제 기기에서 반드시 확인 필요 — `AUDIT.md`의 "환경 제약" 참고)
- [ ] 404 페이지(`/#/존재하지않는주소`)가 정상적으로 뜨는지 확인
- [ ] 관리자 비밀번호를 기본값(`8001`)에서 변경했는지 최종 확인
