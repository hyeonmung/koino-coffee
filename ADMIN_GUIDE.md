# KOI COFFEE 운영 가이드

개발자 없이 사이트를 직접 운영하기 위한 안내입니다. 코드나 터미널을 열 필요 없이, 웹
브라우저에서 관리자 화면(`/admin`)만으로 아래 작업을 모두 할 수 있습니다.

## 시작하기

1. 사이트 아무 페이지든 맨 아래(Footer)까지 스크롤 → 오른쪽 하단 "관리자" 클릭.
2. 비밀번호 입력 (기본값 `8001`. 바꾸려면 개발자에게 요청 — 코드 수정이 필요합니다).
3. 로그인은 브라우저 탭 단위로 유지됩니다. 새 탭이나 다른 컴퓨터에서는 다시 입력해야
   합니다.
4. **중요**: 저장한 모든 데이터는 지금 사용 중인 이 브라우저에만 저장됩니다(LocalStorage).
   다른 컴퓨터나 시크릿 모드에서는 보이지 않습니다. 항상 같은 컴퓨터, 같은 브라우저로
   접속해 운영하세요. (여러 매장/직원이 동시에 관리해야 한다면 개발자에게 Supabase 연결을
   요청하세요 — README의 "데이터베이스로의 전환" 참고.)

## 자주 하는 일 1 — 새 원두 등록하고 공개하기

원두 하나를 등록하는 순서는 **"01 BASIC → 02 FLAVOR → 03 SENSORY → 04 CHARACTER"** 흐름을
따르는 것을 권장합니다. Flavor Notes와 Sensory를 먼저 입력해두면, 04 CHARACTER 탭에서
시스템이 어울리는 Character를 자동으로 추천해줍니다.

1. 대시보드 또는 상단 "원두 관리" → **+ 새 원두 등록**.
2. "01 BASIC"에서 원두명(영문)과 국가 등 기본 정보를 입력합니다.
3. "02 FLAVOR"에서 향미(Flavor Notes)를 입력합니다. Mango, Jasmine처럼 원두에서
   느껴지는 향을 자유롭게 추가하세요.
4. "03 SENSORY"에서 산미·단맛·바디·여운·플레이버·접근성 6개 항목을 슬라이더로
   입력합니다. 입력하는 즉시 오른쪽 "Live Preview"의 Radar가 실시간으로 바뀝니다.
5. "04 CHARACTER"로 이동하면 **"시스템 추천"** 카드가 나타납니다 — 입력한 Flavor Notes를
   기준으로 CLEAR/VIVID/JUICY/CALM/ELEGANT 중 가장 어울리는 Character와 그 이유("Mango,
   Pineapple이 JUICY Flavor Family와 높은 관련성을 가집니다" 같은 문구), 5개 Character의
   점수를 함께 보여줍니다. **"추천대로 적용" 버튼을 누르거나, 아래 5개 버튼에서 직접
   Character를 선택**하세요 — 추천은 참고용일 뿐, 최종 판단은 항상 운영자의 몫입니다.
6. Character를 확정하면 그 즉시 Character 색상(텍스트·Flavor Notes·Radar 포인트)이 사이트
   전체에 자동으로 적용됩니다 — 페이지마다 따로 색을 지정할 필요가 없습니다.
7. 사진이 있다면 "10 MEDIA" 탭에서 대표 이미지 URL을 입력하세요. **사진 URL이 없으면
   손님 화면에는 KOI 브랜드 플레이스홀더(남색 배경 + 별 + 원두 이름)가 대신 표시됩니다 —
   빈 화면이 되지 않으니 사진이 아직 없어도 먼저 등록해도 됩니다.**
8. "07 ROAST" 탭에서 로스터 코멘트, 바리스타 코멘트, (있다면) 세부 로스팅 데이터를
   입력하면 "원두 차트" 페이지에 바로 반영됩니다.
9. "12 PUBLISH" 탭에서 **상태를 "공개"로 변경**해야 손님이 볼 수 있습니다. "초안"은
   관리자만 볼 수 있습니다.
10. 우측 상단 "저장" 클릭. 저장 즉시 손님 사이트에 반영됩니다 — 별도 배포/재시작이 필요
    없습니다.
11. 홈 화면 "지금 만날 수 있는 커피"에 노출하려면 "원두 관리" 목록에서 별표(★)를 클릭해
    Featured로 지정하세요. 목록의 ▲▼ 버튼으로 노출 순서를 바꿀 수 있습니다(최대 4개
    표시). 홈 카드에는 Sensory 6개 값을 보여주는 작은 Radar가 자동으로 함께 표시됩니다 —
    따로 설정할 필요가 없습니다.

**"원두 관리" 목록에서 Character 아래 작은 "제안: XXXX" 문구**가 보이면, 시스템이 계산한
추천 Character가 현재 저장된 값과 다르다는 뜻입니다(예: Flavor Notes를 나중에 수정한
경우). 참고만 하시고, 바꾸고 싶지 않다면 무시해도 됩니다 — 자동으로 바뀌지 않습니다.

## 자주 하는 일 1.5 — 메인 화면 상단(스포트라이트)에 소식 올리기

홈 화면 Hero 오른쪽의 큰 사진 영역은 "메인 스포트라이트"입니다. 3초마다 자동으로 다음
소식으로 넘어가는 슬라이드쇼로, 원두 소개뿐 아니라 공지·이벤트·이야기·영상·교육·납품
소식까지 무엇이든 올릴 수 있습니다.

1. "메인 스포트라이트" 메뉴 → **+ 새 스포트라이트**.
2. 콘텐츠 종류를 고르세요.
   - **원두 / 콘텐츠 / 브루 가이드**를 고르면 "연결할 항목" 드롭다운이 나타납니다. 이미
     등록된 원두·이야기·브루 가이드 중 하나를 고르고 **"불러오기"**를 누르면 제목과
     사진이 자동으로 채워집니다 — 같은 내용을 두 번 입력할 필요가 없습니다.
   - **공지 / 이벤트 / 영상 / 교육 / 납품 / 자유 배너**는 제목·설명·사진을 직접
     작성합니다.
3. 사진(Desktop/Mobile URL), 버튼 문구, 이동 링크, 사진 위 어둡기(낮음/보통/높음)를
   설정합니다. 오른쪽 미리보기에서 즉시 확인할 수 있습니다.
4. 이벤트처럼 기간이 정해진 소식은 "공개 기간"에 시작일·종료일을 입력하세요. 기간이
   지나면 자동으로 슬라이드에서 빠집니다. 비워두면 계속 노출됩니다.
5. 오른쪽 위 "공개" 체크 후 "저장".
6. 목록 화면에서 ▲▼로 순서를 바꾸고, 필요 없어지면 "비공개"로 감추거나 삭제하세요.

**공개 중인 스포트라이트가 하나도 없으면** Featured 원두 1개가 자동으로 대신 표시되므로
화면이 비어 보이는 일은 없습니다. 처음에는 3~6개 정도만 등록하는 것을 권장합니다 —
너무 많으면 손님이 원하는 소식을 보기 전에 다음 슬라이드로 넘어가 버립니다.

## 자주 하는 일 2 — 원두를 판매 종료 처리하기

삭제하지 마세요 — 삭제하면 QR/링크가 모두 깨지고, 예전에 그 원두를 산 손님이 다시 찾아볼
수도 없습니다. 대신:

1. "원두 관리"에서 해당 원두의 상태를 **"보관"**으로 변경.
2. 손님 화면에서는 자동으로 "지난 커피(Past Coffees)"로만 분류되어, 기본 목록/차트에서는
   숨겨지고 "지난 커피도 함께 보기" 토글을 켠 손님에게만 보입니다.

## 자주 하는 일 3 — 메인 화면(홈) 문구·사진 바꾸기

1. "홈 관리" 메뉴.
2. "01 · Hero" 섹션에서 제목/부제목/버튼 문구·링크를 바로 수정할 수 있습니다.
3. "Hero Image URL"에 사진 주소를 넣으면 텍스트 전용 화면 대신 사진이 있는 풀스크린
   히어로로 자동 전환됩니다. 비워두면 지금의 깔끔한 타이포그래피 화면이 유지됩니다.
4. 아래 "섹션 공개/숨김"에서 홈 화면의 각 섹션(지금 만날 수 있는 커피, KOI CUP CHARACTER,
   원두 차트 미리보기 등)을 필요에 따라 켜고 끌 수 있습니다. 순서는 바꿀 수 없습니다
   (실수로 레이아웃이 깨지는 것을 막기 위한 의도된 제한입니다).
5. 우측 상단 "저장" → 즉시 반영.

## 자주 하는 일 4 — 새 용어를 커피 사전에 추가하기

1. "커피 사전" 메뉴 → **+ 새 용어 추가**.
2. 반드시 아래 3단계 순서로 작성하세요 (KOI 사이트 전체의 원칙입니다):
   - **뜻 · 쉽게 말하면**: 전문 용어를 한 문장으로 풀어서.
   - **이런 느낌입니다**: 조금 더 자세한 설명(선택).
   - **예를 들면**: 손님이 실제로 아는 것에 빗댄 예시(선택이지만 강력 권장). 예: "얼그레이
     티에서 느낄 수 있는 향을 떠올리면 이해하기 쉽습니다."
3. 저장하면 즉시 "커피 사전" 페이지와 해당 용어의 향미가 실제로 들어간 원두 페이지에
   자동으로 연결되어 나타납니다 — 직접 연결 작업을 할 필요가 없습니다.

## 자주 하는 일 5 — 이야기(블로그) 작성하기

1. "이야기" 메뉴 → **+ 새 스토리**.
2. 카테고리, 제목, 요약, 본문을 작성합니다. 본문은 일반 텍스트입니다 — **빈 줄로 문단을
   구분**하고, 소제목은 줄 앞에 `## `을 붙이세요 (예: `## 첫 번째 이야기`).
3. "커버 이미지 URL"에 사진을 넣으면 목록 카드와 상세 페이지 상단에 표시됩니다. 비워두면
   KOI 브랜드 플레이스홀더가 대신 나옵니다.
4. 상태를 "공개"로 바꾸고 저장하세요.

## 자주 하는 일 6 — 문의 확인하기

1. "문의 관리" 메뉴에서 `/business`(납품·교육) 페이지로 들어온 문의를 확인합니다.
2. 확인했으면 상태를 "확인함"으로 바꿔 다른 문의와 구분하세요.
3. 처리가 끝난 문의는 "보관"으로 옮기거나 삭제할 수 있습니다(삭제는 2단계 확인 필요).

## 자주 하는 일 7 — 매장 정보(주소·전화·영업시간) 수정하기

1. "사이트 설정" 메뉴 → "연락처 · 링크" 섹션.
2. 전화번호, 주소, **운영시간**, Instagram/Naver 링크, 온라인 구매 링크, **사업자 정보**
   (상호·대표자·사업자등록번호)를 입력합니다.
3. 저장하면 모든 페이지 하단 Footer에 즉시 반영됩니다. 값이 비어 있는 항목은 Footer에
   아예 표시되지 않습니다(빈 칸이나 "정보 없음" 문구가 뜨지 않습니다).

## 사진에 대해 꼭 알아둘 것

- 이 사이트는 사진 **파일 업로드를 지원하지 않습니다.** 대신 이미 인터넷 어딘가에 올라간
  사진의 **주소(URL)**를 입력하는 방식입니다. 예를 들어 Instagram, 네이버 블로그, 구글
  드라이브(공개 링크), 또는 별도 이미지 호스팅 서비스에 먼저 사진을 올린 뒤, 그 사진의
  직접 링크(대개 `.jpg`나 `.png`로 끝남)를 복사해 붙여넣으세요.
- 사진 URL을 넣지 않아도 사이트가 비어 보이지 않습니다 — 남색 배경에 작은 별, 원두/브랜드
  이름이 들어간 KOI 전용 플레이스홀더가 자동으로 표시됩니다.
- 실제 사진이 아닌 인터넷에서 아무 이미지나(저작권 문제가 있는 이미지, 다른 브랜드 사진 등)
  가져다 쓰지 마세요.

## 절대 하지 말아야 할 것

- **원두/스토리를 삭제하지 마세요** — 판매 종료는 "보관" 상태로. 삭제는 QR 코드와 외부
  링크(SNS에 공유한 링크 등)를 전부 깨뜨립니다.
- **존재하지 않는 정보를 지어내 입력하지 마세요** — 로스팅 데이터, 생산자 정보, 인증 등
  확인되지 않은 내용은 비워두세요. 빈 칸은 손님 화면에서 자동으로 숨겨집니다.
- **Character(CLEAR/VIVID/JUICY/CALM/ELEGANT) 5개는 추가·삭제할 수 없습니다** — 이름 자체를
  바꿔야 한다면 개발자에게 요청하세요(코드 수정 필요).

## 문제가 생겼을 때

- **저장이 안 될 때**: 필수 항목(원두명, Country 등 `*` 표시)이 비어 있지 않은지 확인하세요.
- **손님 화면에 안 보일 때**: 상태가 "공개"로 되어 있는지 확인하세요. "초안"/"보관"은
  관리자에게만 보입니다.
- **다른 컴퓨터에서 데이터가 안 보일 때**: 정상입니다 — 이 사이트는 브라우저별로 데이터를
  저장합니다. 항상 같은 컴퓨터·브라우저로 접속하세요.
- 이 문서에서 해결되지 않는 문제는 개발자에게 문의하세요. `README.md`의 "남아 있는
  제한사항"에 이 사이트가 아직 지원하지 않는 기능(파일 업로드, 여러 관리자 동시 편집,
  예약 발행 등)이 정리되어 있습니다.
